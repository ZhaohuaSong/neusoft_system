from django.apps import AppConfig


class SecurityConfig(AppConfig):
    name = 'richDataX-system.security'
    verbose_name = "security"

    def ready(self):
        """Override this to put in:
            Users system checks
            Users signal registration
        """
        pass
