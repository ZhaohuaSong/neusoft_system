import json


def get_y_msg(msg):
    resp_msg = {'code': '00', 'desc': msg}
    return json.dumps(resp_msg)


def get_n_msg(msg):
    resp_msg = {'code': '-1', 'desc': msg}
    return json.dumps(resp_msg)
